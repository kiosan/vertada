<%=image_tag('tag.png', :class=>"tag_image") if tags.length > 0%>
<%=
 idea ||= nil
 search_url ||= false
 sharings ||= false
 
 tags.collect {|tag| 
		res = link_to(tag.name, (!idea.nil? || search_url) ? tags_filter_path(tag.id) : tag_toggle_uri(tag.id), {:style=>tag.style} )
    res
	}.join(", ")
%>