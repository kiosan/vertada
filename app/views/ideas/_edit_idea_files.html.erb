
<ul>
  <%files.each do |file|%>
    <li><%=link_to file.file_name, file_path(:id=>file.id, :name=>file.file_name)%>
    <%if idea_id and (file.uploader==current_user)%>
        <%=link_to_remote "Delete"[:delete], :url=>delete_file_path(file.id, idea_id), :html=>{:class=>'delete_file_link'}, :confirm=>"Sure?"%>
      <%end%>
      </li>
  <%end%>
</ul>